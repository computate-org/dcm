apiVersion: route.openshift.io/v1
kind: Route
metadata:
  namespace: dcm
  name: dcm
spec:
  host: dcm.apps-crc.testing
  to:
    kind: Service
    name: dcm
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
